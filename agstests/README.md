## AGS3D GDScript Test Suite

Run headlessly from repo root:

```sh
./bin/godot.linuxbsd.editor.x86_64 --headless --path agstests --script run_tests.gd
```

Exit code 0 = all tests passed. Exit code 1 = at least one failure.
